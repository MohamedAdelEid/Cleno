/** Fixed Y-axis ceiling for the daily order volume chart. Adjust as the business scales. */
export const ORDER_VOLUME_Y_AXIS_MAX = 80

/** Number of Y-axis labels including zero (e.g. 5 → 0, 75, 150, 225, 300). */
export const ORDER_VOLUME_Y_AXIS_TICK_COUNT = 5
